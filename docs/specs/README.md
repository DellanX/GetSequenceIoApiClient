# Specs Directory

This directory mirrors repository structure so each module/area can hold implementation-facing specs.

Primary use case:
- compare `openapi.json` endpoint contracts against the current Python client implementation,
- identify implemented vs missing endpoints before adding/changing API functionality.

Current API endpoint specs live under:
- `GetSequenceIoApiClient/accounts/README.md`
- `GetSequenceIoApiClient/activity/README.md`
- `GetSequenceIoApiClient/rules/README.md`
- `GetSequenceIoApiClient/audit_logs/README.md`
